<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/tianji"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Secret"
        tools:targetApi="31">
        <activity android:name=".Activities.HomePage"
            android:exported="false"/>
        <activity android:name=".Activities.SubModule.MultiThread"
            android:exported="false"/>
        <activity android:name=".Activities.SubModule.PocketBook"
            android:exported="false"/>
        <activity
            android:name=".Activities.Main"
            android:exported="false"/>
        <activity
            android:name=".Activities.AccountCreation"
            android:exported="false" />
        <activity
            android:name=".Activities.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>