<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <SeekBar
        android:id="@+id/seekbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:maxHeight="@dimen/seekbar_height"
        android:thumb="@null"
        android:background="@null"
        android:paddingStart="@dimen/none"
        android:paddingEnd="@dimen/none"
        android:thumbOffset="@dimen/none"
        android:progressDrawable="@drawable/seekbar"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">
        <EditText
            android:id="@+id/startText"
            android:layout_width="wrap_content"
            android:gravity="start|center_vertical"
            android:layout_height="match_parent"
            android:textSize="@dimen/seekbar_input_size"
            android:paddingStart="@dimen/seekbar_input_padding"
            android:background="@null"
            android:paddingEnd="@dimen/none"
            android:inputType="number"/>
        <TextView
            android:id="@+id/endText"
            android:layout_width="@dimen/none"
            android:layout_weight="1"
            android:gravity="end|center_vertical"
            android:layout_height="match_parent"
            android:textSize="@dimen/seekbar_textview_size"
            android:paddingEnd="@dimen/seekbar_textview_padding"
            android:paddingStart="@dimen/none"/>
    </LinearLayout>
</merge>